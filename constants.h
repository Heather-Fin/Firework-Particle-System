#pragma once

//constants

const float DELTA = 1; // delta time
const int NUMBER_OF_PARTICLES = 1000;
const float LENGTH = 200;

// range of mass for each particle
const int MIN_MASS = 15;
const int MAX_MASS = 25;

const int FORCE_MAG = 1500; // center pull force
